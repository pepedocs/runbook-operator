apiVersion: v1
kind: Pod
metadata:
  name: mysql-missing-secret
  namespace: ro-tests
  labels:
    name: mysql-missing-secret
spec:
  containers:
    - name: mysql
      image: mysql:latest
      env:
        - name: "MYSQL_USER"
          valueFrom:
            secretKeyRef:
              name: "fail"
              key: "user"
        - name: "MYSQL_PASSWORD"
          valueFrom:
            secretKeyRef:
              name: "fail"
              key: "password"
        - name: "MYSQL_DATABASE"
          value: "mysql"
        - name: "MYSQL_ROOT_PASSWORD"
          valueFrom:
            secretKeyRef:
              name: "fail"
              key: "rootpassword"
      ports:
        - containerPort: 3306
